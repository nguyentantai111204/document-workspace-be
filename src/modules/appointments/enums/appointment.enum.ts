export enum AppointmentStatus {
    SCHEDULED = 'scheduled',
    CANCELLED = 'cancelled',
    DONE = 'done',
}

export enum AppointmentType {
    MEETING = 'meeting',
    CALL = 'call',
    OTHER = 'other',
}