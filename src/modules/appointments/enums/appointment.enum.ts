export enum AppointmentStatus {
    SCHEDULED = 'scheduled',
    ONGOING = 'ongoing',
    CANCELLED = 'cancelled',
    DONE = 'done',
}

export enum AppointmentType {
    MEETING = 'meeting',
    CALL = 'call',
    OTHER = 'other',
}